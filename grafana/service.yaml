apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: go-fiber
  labels:
    app: grafana
spec:
  selector:
    app: grafana
  ports:
    - name: grafana
      protocol: TCP
      port: 3000      
      targetPort: 3000
  type: ClusterIP
